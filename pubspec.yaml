name: unfmt
description: An un-formatter for code that was auto formatted on accident.
version: 1.0.0

environment:
  sdk: '>=2.8.1 <3.0.0'

dependencies:
  args: ^1.6.0
  analyzer: any

dev_dependencies:
  pedantic: ^1.9.0
  test: ^1.14.4
